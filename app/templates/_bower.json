{
  "name": "<%= _.slugify(projectName) %>",
  "version": "<%= version %>",
  "dependencies": {<%
    var depn = [];
    depn.push('\n    "jquery": "' + jQLib + '"');
    print(depn.join(','));
  %>
  },
  "directory": "lib"
}