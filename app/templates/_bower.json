{
  "name": "<%= _.slugify(projectName) %>",
  "version": "<%= version %>",
  "dependencies": {<%
    var depn = [];
    if (includeSeajs) {
      depn.push('\n    "seajs": "*"');
    }

    print(depn.join(','));
  %>
  }
}